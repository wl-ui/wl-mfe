/**
 * auth: weilan
 * time: 2017/01/17
 * des: 公共工具类
 */


/* function dissArray(arr1, arr2, key) {
  let arr1_keys = arr1.map(i => i[key]);
  let arr2_keys = arr2.map(i => i[key]);
  let diff_keys = [];
  arr1.forEach(i => {
    let the_same = arr2.some()
  })
} */